{
  "type": "object",
  "required": [
    "metricName"
  ],
  "properties": {
    "metricName": {
      "type": "string"
    },
    "aliases":{
      "$ref": "#/definitions/alias_array"
    }
  },
  "description": "Create a metric type (e.g. table)"
}
