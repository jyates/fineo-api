{
  "type": "object",
  "required": [
    "metricName"
  ],
  "properties": {
    "metricName": {
      "type": "string"
    },
    "aliases":{
      "$ref": "#/definitions/aliasArray"
    }
  },
  "description": "Create a metric type (e.g. table)"
}
