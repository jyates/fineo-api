{
  "type": "object",
  "required": [
    "metricName",
    "fieldName",
    "fieldType"
  ],
  "properties": {
    "metricName": {
      "type": "string"
    },
    "fieldName": {
      "type": "string"
    },
    "fieldType": {
      "type": "string",
      "description": "Standard SQL type for the field. See documentation for valid types."
    },
    "aliases":{
      "$ref": "#/definitions/alias_array"
    }
  },
  "description": "Add a field to a metric type"
}
