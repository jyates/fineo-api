{
  "type": "object",
  "required": [
    "metricName"
  ],
  "properties": {
    "metricName": {
      "type": "string"
    },
    "newDisplayName": {
      "type": "string",
      "description": "Name of the field to use when doing reads"
    },
    "aliases":{
      "$ref": "#/definitions/alias_array"
    },
    "timestampPatterns": {
      "$ref": "#/definitions/timestamp_patterns"
    }
  },
  "description": "Update a metric type"
}
