{
  "type": "object",
  "properties": {
    "timestampPatterns": {
      "$ref": "#/definitions/timestampPatterns"
    },
    "metricKeys": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Alternative keys to identify the metric type. E.g. rather than using the default 'metrictype', you could specifiy 'myfield' and have the column: 'myfield' : 'mymetric'. See documentation for more information on how this works."
    }
  },
  "description": "Update top level schema handling"
}
